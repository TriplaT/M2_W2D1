using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Esercizio3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Nei Commenti la spiegazione");
        /*
         * Per esporre una variabile nell'inspector abbiamo vari metodi:
         * Metodo 1: 
         * uso della visibilità 'public' ES                     public int myVar = 0;
         * Metodo 2:
         * uso della visibilità '[SerializeField]' ES           [SerializeField] private int myVar = 0;
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
